{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the main window \u2014 core APIs + explicit window dragging permission required by Tauri 2 for startDragging() and data-tauri-drag-region to work.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize",
    "updater:default",
    "process:default",
    "global-shortcut:default",
    "notification:default",
    "core:webview:allow-set-webview-zoom"
  ]
}
